// Central API configuration
// The Vite dev proxy handles routing to the correct backend port.
// All requests use relative paths (no hardcoded host/port).

export const API_BASE = '';  // empty = use relative paths (proxied by Vite)
export const WS_BASE = '';   // empty = use relative paths (proxied by Vite)
